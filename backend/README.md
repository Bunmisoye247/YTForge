# ytforge (backend)

Backend package metadata readme — hatchling requires this file inside the
project directory (`backend/`), so it can't just point at the repo root
`../README.md`. See [../README.md](../README.md) for full project docs.
